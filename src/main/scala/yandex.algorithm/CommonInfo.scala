package yandex.algorithm

case class CommonInfo(siteCount: Int,
                      themeCount: Int,
                      robotCount: Int,
                      simulationTime: Int,
                      oldCoeff: Int)
